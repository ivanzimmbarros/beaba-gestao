__pycache__/

# Variáveis locais / staging — nunca enviar `.env` com segredos
.env
.env.staging
.env.production

# E20 — relatório gerado por stress E2E (reexecutável localmente)
tests/last_stress_report.txt

# SQLite — todos os .db ficam fora do Git (intencional):
#   - data/beaba_gestao.db (operacional, vivo) nunca versionado;
#   - backups/hourly/*.db e backups/cloud_queue/*.db (réplicas para DR) também *.db —
#     sincronização «nuvem» = OneDrive/rclone/S3 na pasta backups/ ou artefactos GHA,
#     não commit em origin.
*.db

# E17 — logs de backup (réplicas .db em backups/ já cobertas por *.db)
backups/logs/*.log

# E17.1 — estado efémero GHA e blobs encriptados locais
gha_backup_state.json
gha_restore_state.json
*.beaba.enc

venv/
.venv/
