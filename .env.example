# Variáveis não-secretas (copiar para `.env` local). Segredos reais: GitHub Secrets / gestor de credenciais.
# BEABA_REPO_ROOT=          # opcional — raiz do projecto para scripts de backup local
# BEABA_BACKUP_KEEP=168
# BEABA_BACKUP_CLOUD_QUEUE=1
