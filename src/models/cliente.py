import re import sqlite3 from src.database.connection import get_connection  def validar_e_limpar_whatsapp(whatsapp):     num_limpo = re.sub(r'\D', '', whatsapp)     return num_limpo if len(num_limpo) == 11 else None  def cadastrar_cliente(nome, whatsapp):     whatsapp_limpo = validar_e_limpar_whatsapp(whatsapp)     if not whatsapp_limpo:         return False, "❌ Erro: O WhatsApp deve conter 11 dígitos numéricos."     if not nome.strip():         return False, "❌ Erro: O nome é obrigatório."     conn = get_connection()     try:         cursor = conn.cursor()         cursor.execute("INSERT INTO clientes (nome, whatsapp) VALUES (?, ?)", (nome.strip(), whatsapp_limpo))         conn.commit()         return True, "✅ Sucesso: Cliente cadastrado!"     except sqlite3.IntegrityError:         return False, "⚠️ Aviso: Este WhatsApp já existe."     finally:         conn.close()